# from django.shortcuts import render
# from .models import Beneficiary
#
# def beneficiaries(request):
#     beneficiaries = Beneficiary.objects.all()
#     context = {
#         'beneficiaries': beneficiaries
#     }
#     return render(request, 'beneficiaries.html', context)


"""

This assumes that you have a Beneficiary model in your Django project with the fields name,
 amount, and status. You may 
need to adjust the field names in the HTML template and view function to match your specific use case.

"""